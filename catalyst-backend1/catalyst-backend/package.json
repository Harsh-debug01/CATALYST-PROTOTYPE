{
  "name": "catalyst-backend",
  "version": "1.0.0",
  "description": "Minimal backend for Catalyst — proxies frontend requests to the Anthropic API so the key never ships to the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
